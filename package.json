{
  "name": "jack-portfolio",
  "private": true,
  "type": "module",
  "description": "Self-updating dashboard of live prices across both personal portfolios (keyless, no API keys).",
  "scripts": {
    "collect": "node scripts/collect.mjs"
  }
}
